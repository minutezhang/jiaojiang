1.1 powerof4
1.2 iterator filter 5
1.3 intersection of two lists

2.1 live game in 1 dimension
2.2 design editor. 
    insert(text, position), delete(fromPositin, toPosition), highlight(fromPositin, toPosition), unhighlight(fromPosition, toPosition), replaceWith(text, position)
    undo/redo


3.1 debug multipmap

4.1 array is better than hash (cache)
4.2 delete node with target value in linked list

5.1 print triangle with given edge length

6.1 chat
